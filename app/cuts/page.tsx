import type {Metadata} from "next"; import {PageHero,SectionHead} from "@/components/ui"; import {ServiceList} from "@/components/ServiceList"; import {PriceBuilder} from "@/components/PriceBuilder"; import {services} from "@/data/site";
export const metadata:Metadata={title:"Cuts & Prices",description:"Browse CUT/CTRL cuts, fades, beard work, packages and student pricing."};
export default function Cuts(){const cats=[...new Set(services.map(s=>s.category))];return <><PageHero number="01" title="Cuts / prices" copy="Clear work. Clear timing. No mystery add-ons."/><section className="section"><SectionHead number="01" title="The service file" aside={`${cats.length} categories / pricing from £12`}/><ServiceList limit={services.length}/><div className="mt-8 grid gap-px border border-black bg-black md:grid-cols-4">{cats.map(c=><div key={c} className="bg-[var(--paper)] p-4"><span className="meta blue">Category</span><h3 className="display mt-3 text-3xl">{c}</h3><p className="text-sm">{services.filter(s=>s.category===c).map(s=>s.name).join(' / ')}</p></div>)}</div></section><section className="section border-t border-black"><SectionHead number="02" title="Build your service" aside="Mix the work. See price and chair time instantly."/><PriceBuilder/></section></>}
