"use client"; import Link from "next/link"; import {usePathname} from "next/navigation"; import {useState} from "react"; import {X} from "lucide-react";
export function MobileBookBar(){const path=usePathname();const [show,setShow]=useState(true);if(!show||path.startsWith('/book'))return null;return <div className="fixed inset-x-0 bottom-0 z-40 flex items-center gap-3 border-t border-black bg-[var(--paper)] p-3 pb-[max(.75rem,env(safe-area-inset-bottom))] md:hidden"><div className="meta flex-1">Cuts from<br/><b className="text-base">£24</b></div><Link className="bg-[var(--blue)] px-6 py-3 font-bold uppercase text-white" href="/book">Book now</Link><button type="button" aria-label="Dismiss booking bar" onClick={()=>setShow(false)}><X aria-hidden="true"/></button></div>}
