"use client"; import Image from "next/image"; import {useState} from "react"; import {services} from "@/data/site"; import {ArrowUpRight} from "lucide-react";
export function ServiceList({limit=8}:{limit?:number}){const [active,setActive]=useState(0);const items=services.slice(0,limit);return <div className="grid border border-black lg:grid-cols-[1fr_36%]"><div>{items.map((s,i)=><button key={s.code} onMouseEnter={()=>setActive(i)} onFocus={()=>setActive(i)} onClick={()=>setActive(i)} className={`group grid w-full grid-cols-[45px_1fr_auto] items-center border-b border-black p-3 text-left last:border-b-0 md:grid-cols-[56px_1fr_1.4fr_75px_70px_45px] ${active===i?'bg-black text-white':''}`}><span className="meta">{String(i+1).padStart(2,'0')}</span><strong className="display text-xl tracking-tight md:text-2xl">{s.name}</strong><span className="hidden text-sm md:block">{s.description}</span><span className="meta hidden md:block">{s.duration} min</span><span className="meta">£{s.price}</span><ArrowUpRight className="arrow size-5"/></button>)}</div><div className="relative hidden min-h-[520px] overflow-hidden border-l border-black lg:block"><Image src={items[active].image} alt={`${items[active].name} haircut preview`} fill sizes="36vw" className="object-cover grayscale"/><div className="absolute inset-x-0 bottom-0 flex justify-between bg-black p-4 text-white"><span className="display text-2xl">{items[active].name}</span><span className="meta">Preview {active+1}/{items.length}</span></div></div></div>}
