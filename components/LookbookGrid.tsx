"use client"; import Image from "next/image"; import {useEffect,useState} from "react"; import {looks} from "@/data/site"; import {Bookmark,ChevronLeft,ChevronRight,X} from "lucide-react";
const categories=['All','Fades','Texture','Classic','Beard','Creative','Studio'];
export function LookbookGrid({preview=false}:{preview?:boolean}){const [filter,setFilter]=useState('All');const [active,setActive]=useState<number|null>(null);const [saved,setSaved]=useState<number[]>([]);useEffect(()=>{const frame=requestAnimationFrame(()=>{try{setSaved(JSON.parse(localStorage.getItem('cutctrl-saved')||'[]'))}catch{}});return()=>cancelAnimationFrame(frame)},[]);const toggle=(id:number)=>setSaved(s=>{const n=s.includes(id)?s.filter(x=>x!==id):[...s,id];localStorage.setItem('cutctrl-saved',JSON.stringify(n));return n});const items=(filter==='All'?looks:looks.filter(x=>x.category===filter)).slice(0,preview?3:undefined);const move=(d:number)=>setActive(a=>a===null?null:(a+d+items.length)%items.length);useEffect(()=>{const key=(e:KeyboardEvent)=>{if(active===null)return;if(e.key==='Escape')setActive(null);if(e.key==='ArrowRight')move(1);if(e.key==='ArrowLeft')move(-1)};addEventListener('keydown',key);return()=>removeEventListener('keydown',key)});return <><div className="mb-6 flex flex-wrap gap-2">{!preview&&categories.map(c=><button key={c} onClick={()=>setFilter(c)} className={`border border-black px-4 py-2 text-xs font-bold uppercase ${filter===c?'bg-black text-white':''}`}>{c}</button>)}{!preview&&saved.length>0?<span className="ml-auto border border-[var(--blue)] px-4 py-2 text-xs font-bold uppercase text-[var(--blue)]">Saved looks: {saved.length}</span>:null}</div><div className="grid auto-rows-[240px] grid-cols-1 gap-3 md:grid-cols-3">{items.map((look,i)=><article key={look.id} className={`group relative overflow-hidden border border-black ${i===0?'md:col-span-2 md:row-span-2':i===3?'md:row-span-2':''}`}><button onClick={()=>setActive(i)} className="absolute inset-0 z-10" aria-label={`Open ${look.cut} lightbox`}/><Image src={look.image} alt={`${look.cut} by ${look.barber}`} fill sizes="(max-width:768px) 100vw, 66vw" className="object-cover grayscale transition duration-500 group-hover:scale-[1.03] group-hover:grayscale-0"/><div className="absolute inset-x-0 bottom-0 flex items-end justify-between bg-black/90 p-4 text-white"><div><span className="meta text-[var(--blue)]">Look {String(look.id).padStart(2,'0')}</span><h3 className="display text-3xl">{look.cut}</h3><p className="text-xs">By {look.barber}</p></div><button className="relative z-20 p-3" onClick={e=>{e.stopPropagation();toggle(look.id)}} aria-label={`${saved.includes(look.id)?'Remove':'Save'} ${look.cut}`}><Bookmark className={saved.includes(look.id)?'fill-[var(--blue)] text-[var(--blue)]':''}/></button></div></article>)}</div>{active!==null?<div role="dialog" aria-modal="true" aria-label={`${items[active].cut} details`} className="fixed inset-0 z-[70] grid place-items-center bg-black/90 p-4"><div className="relative grid max-h-[90vh] w-full max-w-5xl overflow-auto bg-[var(--paper)] md:grid-cols-[1.4fr_1fr]"><div className="relative min-h-[55vh]"><Image src={items[active].image} alt={items[active].cut} fill className="object-cover" sizes="70vw"/></div><div className="flex flex-col p-6"><button className="self-end p-2" onClick={()=>setActive(null)} aria-label="Close lightbox"><X/></button><span className="meta blue">{items[active].category}</span><h2 className="display mt-3 text-6xl">{items[active].cut}</h2><dl className="mt-8 grid gap-4"><div><dt className="meta">Barber</dt><dd>{items[active].barber}</dd></div><div><dt className="meta">Hair type</dt><dd>{items[active].hair}</dd></div><div><dt className="meta">Styling</dt><dd>{items[active].notes}</dd></div><div><dt className="meta">Maintenance</dt><dd>{items[active].maintenance}</dd></div></dl><div className="mt-auto flex gap-2 pt-8"><button className="border border-black p-3" onClick={()=>move(-1)} aria-label="Previous look"><ChevronLeft/></button><button className="border border-black p-3" onClick={()=>move(1)} aria-label="Next look"><ChevronRight/></button><button className="ml-auto border border-black p-3" onClick={()=>toggle(items[active!].id)}><Bookmark className="mr-2 inline size-4"/>Save</button></div></div></div></div>:null}</>}
