"use client"; import Image from "next/image"; import Link from "next/link"; import {useState} from "react"; import {barbers} from "@/data/site"; import {ArrowUpRight,Star} from "lucide-react";
export function CrewAccordion(){const [active,setActive]=useState(1);return <div className="grid gap-px border border-black bg-black md:flex md:h-[620px]">{barbers.map((b,i)=><article key={b.name} onMouseEnter={()=>setActive(i)} className={`relative min-h-[400px] overflow-hidden bg-black text-white transition-[flex] duration-500 ${active===i?'md:flex-[2.4]':'md:flex-1'}`}><button className="absolute inset-0 z-10 w-full text-left" aria-label={`Show ${b.name}`} aria-expanded={active===i} onFocus={()=>setActive(i)} onClick={()=>setActive(i)}/><Image src={b.image} alt={`${b.name}, CUT/CTRL barber`} fill sizes="(max-width:768px) 100vw, 35vw" className={`object-cover transition duration-500 ${active===i?'opacity-60 grayscale-0':'opacity-65 grayscale'}`}/><span className="display absolute left-4 top-4 z-20 text-2xl">{b.number}</span><div className="absolute inset-x-0 bottom-0 z-20 bg-gradient-to-t from-black via-black/90 to-transparent p-5 pt-24"><h3 className="display text-5xl">{b.name}</h3>{active===i?<div className="grid gap-4 pt-3 md:grid-cols-2"><div><p className="meta text-white/60">Specialty</p><p>{b.specialty}</p><p className="mt-3 flex items-center gap-1"><Star className="size-4 fill-[var(--blue)] text-[var(--blue)]"/>{b.rating}</p></div><div><p>{b.quote}</p><Link className="relative z-30 mt-4 inline-flex items-center gap-3 border border-white p-3 text-xs font-bold uppercase" href="/book">Book with {b.name.split(' ')[0]}<ArrowUpRight className="size-4"/></Link></div></div>:null}</div></article>)}</div>}
