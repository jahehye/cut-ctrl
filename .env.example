# Public origin used for canonical metadata, social previews, sitemap, robots, and JSON-LD.
# Replace with the final HTTPS URL in Vercel. No secrets are required by this project.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
